/**
 * testing basic usage of frisby and jasmine
 */
'use strict';
var config = require('../../config');
var frisby = require('frisby');

/*frisby.create('just testing the server is up')
    .get(config.fullhostname+'/api')
    .expectStatus(200)
    .toss();*/